services:
  postgres:
    image: pgvector/pgvector:pg16
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: functhis
      POSTGRES_PASSWORD: functhis
      POSTGRES_DB: functhis
    volumes:
      - functhis-postgres:/var/lib/postgresql/data

volumes:
  functhis-postgres:

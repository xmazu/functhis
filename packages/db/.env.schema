# @defaultRequired=false
# @defaultSensitive=true
# @import(../../apps/web/, pick=[NODE_ENV])
# @generateTsTypes(path=./src/env.ts, exposeEnv=local)
# ---

# Postgres URL for drizzle-kit migrations. Same file powers local dev via scripts/run-with-local-database-url.ts.
# @type=string
DATABASE_URL=

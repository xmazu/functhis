{
  "name": "@functhis/infra",
  "private": true,
  "type": "module",
  "scripts": {
    "check-types": "echo 'no types'",
    "db:migrate:local": "bun run --filter @functhis/db db:migrate:deploy",
    "deploy:preview": "wrangler deploy --env preview -c ../../apps/web/wrangler.jsonc && wrangler deploy --env preview -c ../../apps/console/wrangler.jsonc",
    "deploy:production": "wrangler deploy --env production -c ../../apps/web/wrangler.jsonc && wrangler deploy --env production -c ../../apps/console/wrangler.jsonc",
    "dev:workers": "wrangler dev -c ../../apps/web/wrangler.jsonc -c ../../apps/console/wrangler.jsonc",
    "tf:preview": "terraform apply -var-file=preview.tfvars",
    "tf:production": "terraform apply -var-file=production.tfvars",
    "tf:validate": "terraform validate"
  },
  "devDependencies": {
    "@functhis/config": "workspace:*",
    "wrangler": "^4.40.2"
  }
}

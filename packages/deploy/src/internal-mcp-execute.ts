/** Hostname used for MCP service-binding requests (not a public route). */
export const INTERNAL_MCP_EXECUTE_HOST = 'internal';

export const internalMcpExecuteUrl = `https://${INTERNAL_MCP_EXECUTE_HOST}/internal/execute`;

# functhis SDK

Optional helpers shipped in the `functhis` npm package. They are not used by the CLI binary.

- `functhis/sdk/next` — Next.js route handler for app-owned function endpoints
- `functhis/sdk/client` — typed HTTP client for published functions that call those endpoints

See [README.md](../README.md#calling-your-app-nextjs).
